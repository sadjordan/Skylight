Main objectives:
- Better application file structure; split by responsibility
- Refactor Command System
    - Current if-else system is getting lengthy
    - Command registry system

To be developed:
- Package long and extensive features that are implemented in the "if-else" system into functions.
- Package functions into classes
- Split classes into different python files
- Refactor if-else system for a more efficient and scalable command system

Notes:

Known issues:

To be implemented in the future:
